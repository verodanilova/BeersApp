//
//  WidgetColor.swift
//  BeersApp
//
//  Created by Veronica Danilova on 20.03.2021.
//

import SwiftUI

class WidgetColor {
    static let backgroundGradientTop: Color = Color(UIColor.black.withAlphaComponent(0.05))
    static let backgroundGradientBottom: Color = Color(UIColor.black.withAlphaComponent(0.7))
    static let text: Color = Color(UIColor(named: "text") ?? .white)
    static let background: Color = Color(UIColor(named: "background") ?? .white)
    static let separator: Color = Color(UIColor(named: "separator") ?? .white)
}
